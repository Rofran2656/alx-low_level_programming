#include "main.h"
/**
 * add - A function that adds two integers and returns the result
 * @a: num 1 to be computed
 * @b: num 2 to be computed
 * Return: Integer value
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
