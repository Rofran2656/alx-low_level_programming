#include "main.h"
/**
 * main - a function that multiplies two integers
 * @x: first number to multiply
 * @y: second number to multiply the first number
 * Return: result of multiplication of x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
